<?php

## Check http://www.observium.org/docs/config_options/ for documentation of possible settings

// Database config ---  This MUST be configured
$config['db_extension'] = 'mysqli';
$config['db_host']      = 'localhost';
$config['db_user']      = 'observium';
$config['db_pass']      = 'ObsVV3m16';
$config['db_name']      = 'observium';

// Base directory
$config['install_dir'] = "/opt/observium";

// Default community list to use when adding/discovering
$config['snmp']['community'] = array("lcn312019");

// Authentication Model
$config['auth_mechanism'] = "mysql";    // default, other options: ldap, http-auth, please see documentation for config help

// Enable alerter (not available in CE)
// $config['poller-wrapper']['alerter'] = TRUE;

// Set up a default alerter (email to a single address)
$config['email']['default']        = "juancarlos@lacnic.net";
$config['email']['from']           = "Observium <observium@lacnic.net>";
$config['email']['default_only']   = TRUE;

// Habilita datos de sesiones/prefijos BGP
$config['enable_bgp'] = 1;

// Habilita path para logs 
$config['log_dir'] = "/opt/observium/logs";
$config['frontpage']['order'] = array('status_summary', 'device_status_boxes', 'device_status', 'eventlog');

// End config.php

